import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        alignItems: "center",
        flex: 1,
    },
    text: {
        padding: 20,
    },
    item: {
        justifyContent: "center",
        alignItems: "center",
    },
});
