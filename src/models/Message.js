export default class Message {
    constructor(id, title, description, message) {
        this.id = id;
        this.title = title;
        this.description = description;
        this.message = message;
    }
}
